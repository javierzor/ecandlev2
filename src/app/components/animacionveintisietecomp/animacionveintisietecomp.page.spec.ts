import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AnimacionveintisietecompPage } from './animacionveintisietecomp.page';

describe('AnimacionveintisietecompPage', () => {
  let component: AnimacionveintisietecompPage;
  let fixture: ComponentFixture<AnimacionveintisietecompPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AnimacionveintisietecompPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
