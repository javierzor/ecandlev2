import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AnimacionveintitrescompPage } from './animacionveintitrescomp.page';

describe('AnimacionveintitrescompPage', () => {
  let component: AnimacionveintitrescompPage;
  let fixture: ComponentFixture<AnimacionveintitrescompPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AnimacionveintitrescompPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
