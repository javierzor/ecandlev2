import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AnimacionveintiseiscompPage } from './animacionveintiseiscomp.page';

describe('AnimacionveintiseiscompPage', () => {
  let component: AnimacionveintiseiscompPage;
  let fixture: ComponentFixture<AnimacionveintiseiscompPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AnimacionveintiseiscompPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
