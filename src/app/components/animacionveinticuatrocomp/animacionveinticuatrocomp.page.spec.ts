import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AnimacionveinticuatrocompPage } from './animacionveinticuatrocomp.page';

describe('AnimacionveinticuatrocompPage', () => {
  let component: AnimacionveinticuatrocompPage;
  let fixture: ComponentFixture<AnimacionveinticuatrocompPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AnimacionveinticuatrocompPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
