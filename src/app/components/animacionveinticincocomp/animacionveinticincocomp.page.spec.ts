import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AnimacionveinticincocompPage } from './animacionveinticincocomp.page';

describe('AnimacionveinticincocompPage', () => {
  let component: AnimacionveinticincocompPage;
  let fixture: ComponentFixture<AnimacionveinticincocompPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AnimacionveinticincocompPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
