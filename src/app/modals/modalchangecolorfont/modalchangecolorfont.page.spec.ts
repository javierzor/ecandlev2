import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModalchangecolorfontPage } from './modalchangecolorfont.page';

describe('ModalchangecolorfontPage', () => {
  let component: ModalchangecolorfontPage;
  let fixture: ComponentFixture<ModalchangecolorfontPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ModalchangecolorfontPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
