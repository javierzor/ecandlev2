import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ModalchangefontPage } from './modalchangefont.page';

describe('ModalchangefontPage', () => {
  let component: ModalchangefontPage;
  let fixture: ComponentFixture<ModalchangefontPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ModalchangefontPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
